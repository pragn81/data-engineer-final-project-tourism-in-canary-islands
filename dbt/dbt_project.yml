name: "canary_islands_tourism"
version: "1.0.0"
config-version: 2

profile: "canary_islands_tourism"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  canary_islands_tourism:
    staging:
      +schema: tourism_staging
      +materialized: view
    mart:
      +schema: tourism_mart
      +materialized: table
